gateway:
  mirror:
    gateway_config:
      mirror_client:
        addrs:
          - vald-mirror-gateway.vald-01.svc.cluster.local:8081

discoverer:
  clusterRole:
    enabled: false
  clusterRoleBinding:
    name: vald-02
  serviceAccount:
    name: vald-02
