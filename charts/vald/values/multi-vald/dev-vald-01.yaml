gateway:
  mirror:
    gateway_config:
      mirror_client:
        addrs:
          - vald-mirror-gateway.vald-01.svc.cluster.local:8081

discoverer:
  clusterRoleBinding:
    name: vald-01
  serviceAccount:
    name: vald-01
