!function() {
  const e = document.getElementById("menu"),
        t = document.getElementById("nav-btn");
  function n() { e.classList.remove("header__list--open") }
  document.querySelectorAll('a[href^="#"]')
      .forEach(function(e) { e.addEventListener("click", n) }),
      t.addEventListener("click", function() {
        e.classList.contains("header__list--open")
            ? n()
            : e.classList.add("header__list--open")
      });
  const o = document.getElementById("current");
  document.addEventListener("click", o, function() {
    o.classList.contains("open") ? o.classList.remove("open")
                                 : o.classList.add("open")
  });
  const c = document.getElementById("header").offsetHeight;
  let i = (e, t) => {
    let n, o = window.pageYOffset;
    const c = e.getBoundingClientRect().top + o - t;
    !function() {
      let e = arguments.callee;
      n = o + Math.round((c - o) / 8), window.scrollTo(0, n), o = n,
      document.body.clientHeight - window.innerHeight < n
          ? window.scrollTo(0, document.body.clientHeight)
          : n >= c + 5 || n <= c - 5 ? window.setTimeout(e, 10)
                                     : window.scrollTo(0, c)
    }()
  };
  const d = c, l = document.querySelectorAll('a[href*="#"]');
  for (let e = 0; e < l.length; e++)
    l[e].addEventListener("click", function(e) {
      const t = e.currentTarget.getAttribute("href").split("#")[1],
            n = document.getElementById(t);
      return !n || (i(n, d), !1)
    })
}();
