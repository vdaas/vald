[metadata]
name = ann-benchmarks

[options]
zip_safe = False
package_dir =
    =ann-benchmarks
packages = find:
install_requires =
    numpy
    h5py
    requests
    scipy
    docker
    psutil

[options.packages.find]
where=ann-benchmarks
