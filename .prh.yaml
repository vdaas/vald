version: 1
rules:
  # index: ann
  - pattern: ann
    expected: ANN
    specs:
      - from: planning
        to: planning
      - from: ann
        to: ANN
    options:
      wordBoundary: true
  # index: clojure
  - pattern: clojure
    expected: Clojure
    options:
      wordBoundary: true
  # index: docker
  - pattern: docker
    expected: Docker
    options:
      wordBoundary: true
  # index: flamegraph
  - pattern: flamegraph
    expected: Flamegraph
    options:
      wordBoundary: true
  # index: go
  - pattern:
      - Golang
      - golang
      - /go(?! to)/
    expected: Go
    options:
      wordBoundary: true
    specs:
      - from: go
        to: Go
      - from: going
        to: going
      - from: go to
        to: go to
      - from: golang
        to: Go
      - from: algorithm
        to: algorithm
  # index: grafana
  - pattern: grafana
    expected: Grafana
    options:
      wordBoundary: true
  # index: helm
  - pattern: helm
    expected: Helm
    options:
      wordBoundary: true
  # index: jaeger
  - pattern: jaeger
    expected: Jaeger
    options:
      wordBoundary: true
  # index: jave
  - pattern: java
    expected: Java
    options:
      wordBoundary: true
  # index: jupyter
  - pattern:
      - jupyter
      - jupiter
    expected: Jupyter
    options:
      wordBoundary: true
  # index: knn
  - pattern:
      - knn
      - KNN
      - kNN
    expected: _k_-NN
    options:
      wordBoundary: true
  # index: kubernetes
  - pattern: 
      - kubernetes
      - k8s
      - K8s
    expected: Kubernetes
    options:
      wordBoundary: true
  # index: ngt
  - pattern: ngt
    expected: NGT
    specs:
      - from: length
        to: length
      - from: ngt
        to: NGT
    options:
      wordBoundary: true
  # index: ngtd
  - pattern: ngtd
    expected: NGTD
    options:
      wordBoundary: true
  # index: node.js
  - pattern:
      - node.js
      - nodejs
    expected: Node.js
  # index: onnx
  - pattern:
      - onnx
      - ONNX
    expected: ONNX
    options:
      wordBoundary: true
  # index: pyroscope
  - pattern: pyroscope
    expected: Pyroscope
    options:
      wordBoundary: true
  # index: python
  - pattern: python
    expected: Python
    options:
      wordBoundary: true
  # index: sdk
  - pattern: sdk
    expected: SDK
    options:
      wordBoundary: true
  # index: uuid
  - pattern: uuid
    options:
      wordBoundary: true
    expected: UUID
  # index: vald
  - pattern: vald
    expected: Vald
    options:
      wordBoundary: true
    specs:
      - from: vald
        to: Vald
      - from: valdcli
        to: valdcli
  # index: yahoo! japan
  - pattern:
      - Yahoo Japan!
      - Yahoo Japan
      - YJ
    expected: Yahoo! Japan
